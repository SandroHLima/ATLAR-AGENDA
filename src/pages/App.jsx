import { useState } from 'react'; 
import { useNavigate } from 'react-router-dom';

const Api = () => {
  const navigate = useNavigate();

  return (
    <div>
      <h1>welcome</h1>
      </div>
  );
};

export default Api;